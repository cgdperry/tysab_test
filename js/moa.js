// JavaScript Document

//cur_state = 1;

(function ($) { /* closure and portability */
    $(document).ready(function(){
		
		// automatically show the isi...
		// REMOVED 2013 12 27 AP
		//$(".tap_div").trigger("click");

		// Advance to first state. This was originally in a settimeout. It didn't seem to need it.
		//$('.contents').removeClass('state-0').addClass('state-1');
		
		// make sure the state variable keeps up.
		//cur_state = 1;

	
	});
})(jQuery);